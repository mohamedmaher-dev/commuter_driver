part of '../profile_view.dart';

class _FailureBody extends StatelessWidget {
  const _FailureBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.error),
    );
  }
}
